The storefront falls back to the catalog-based seed data in `src/data/site.ts`.

Use that file as the reference when manually importing content into Supabase, or extend the admin to add a dedicated import action later.
